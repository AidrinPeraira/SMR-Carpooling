info:
  name: get-admin-driver-details
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:4000/api/v1/admin/trip/driver/details/:driverId
  headers:
    - name: Authorization
      value: Bearer {{ACCESS_TOKEN}}
    - name: x-frontend-key
      value: "{{FRONTEND_KEY}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
