info:
  name: check-health
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:4001/health
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
