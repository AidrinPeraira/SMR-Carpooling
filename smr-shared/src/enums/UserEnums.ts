

export enum UserRole {
  PASSENGER = 'passenger',
  DRIVER = 'driver',
  ADMIN = 'admin'
}

export enum AccountStatus {
  PENDING_VERIFICATION = "pending_verification",
  VERIFIIED = "verified",
  SUSPENDED = "suspended",
  BLOCKED = "blocked"
}
