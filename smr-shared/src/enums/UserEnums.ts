export enum UserRole {
  PASSENGER = "passenger",
  DRIVER = "driver",
  ADMIN = "admin",
}

export enum AccountStatus {
  PENDING_VERIFICATION = "pending_verification",
  ACTIVE = "active",
  BLOCKED = "blocked",
}
