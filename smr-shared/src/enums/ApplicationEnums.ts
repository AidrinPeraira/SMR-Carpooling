export enum ApplicationType {
  ONBOARDING = "onboarding",
  NEW_VEHICLE = "new_vehicle",
  RENEW_VEHICLE = "renew_vehicle",
  RENEW_DRIVER = "renew_driver",
}

export enum ApplicationStatus {
  PENDING = "pending",
  RETURNED = "returned",
  REJECTED = "rejected",
  APPROVED = "approved",
}
