import { VehicleTypes } from "@sharemyride/shared";

export class VehicleRecordEntity {
  constructor(
    private readonly recordId: string,
    private readonly applicationId: string,
    private readonly vehicleType: VehicleTypes,
    private readonly vehicleModel: string,
    private readonly vehicleMake: string,
    private vehicleImage: string,
    private readonly registrationNumber: string,
    private registrationExpiry: string,
    private registrationFile: string,
    private insuranceNumber: string,
    private insuranceExpiry: string,
    private insuranceFile: string,
    private readonly createdAt: Date,
    private updatedAt: Date,
  ) {
    //add validation for data.
    //throw errors.
  }

  public getFullRecord() {
    //assume this is implemented
    return {} as any;
  }

  public get isRegistrationExpired(): boolean {
    return new Date(this.registrationExpiry) < new Date();
  }

  public get isInsuranceExpired(): boolean {
    return new Date(this.insuranceExpiry) < new Date();
  }
}
