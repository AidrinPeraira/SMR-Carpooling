{
  "extends": "../../tsconfig.base.json", //get all setings form patent ts config
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src",
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["src/**/*.ts"], //compile these files
  "exclude": ["node_modules"] //don't compile these files
}
