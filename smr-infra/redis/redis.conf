# Redis Configuration for Development
bind 0.0.0.0
protected-mode no
port 6379

# Memory Management
maxmemory 256mb
maxmemory-policy allkeys-lru

# Persistence (Optional for dev, but kept minimal)
save ""
appendonly no
